<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Power SMS | SMS RateCard</title>
  <link rel="stylesheet" href="/css/dashboard.css">
</head>
<body>
  <div class="layout">
    <%- include('partials/sidebar', { active: active, user: user }) %>
    <div class="main">
      <%- include('partials/topbar', { pageTitle: pageTitle, user: user }) %>
      <div class="content">

        <% if (user.role !== 'client' && user.role !== 'agent') { %>
        <div class="page-card">
          <h3>Add Rate</h3>
          <form method="POST" action="/ratecard/create">
            <div class="form-row">
              <label>Country / Operator</label>
              <input type="text" name="country" required>
            </div>
            <div class="form-row">
              <label>Prefix</label>
              <input type="text" name="prefix" required>
            </div>
            <div class="form-row">
              <label>Currency</label>
              <input type="text" name="currency" placeholder="USD">
            </div>
            <div class="form-row">
              <label>Rate</label>
              <input type="text" name="rate" placeholder="0.00">
            </div>
            <button type="submit" class="btn-primary">Add Rate</button>
          </form>
        </div>
        <% } %>

        <div class="page-card">
          <h3>SMS RateCard</h3>
          <table class="data-table">
            <thead>
              <tr>
                <th>Country</th>
                <th>Prefix</th>
                <th>Currency</th>
                <th>Rate</th>
                <% if (user.role !== 'client' && user.role !== 'agent') { %><th>Action</th><% } %>
              </tr>
            </thead>
            <tbody>
              <% if (rates.length === 0) { %>
                <tr><td colspan="5">No rates added yet.</td></tr>
              <% } %>
              <% rates.forEach(function(r) { %>
                <tr>
                  <td><%= r.country %></td>
                  <td><%= r.prefix %></td>
                  <td><%= r.currency %></td>
                  <td><%= r.rate %></td>
                  <% if (user.role !== 'client' && user.role !== 'agent') { %>
                  <td>
                    <form method="POST" action="/ratecard/delete/<%= r.id %>" style="display:inline">
                      <button type="submit" class="btn-primary" style="padding:5px 10px;font-size:12px;background:#dc2626;">Delete</button>
                    </form>
                  </td>
                  <% } %>
                </tr>
              <% }); %>
            </tbody>
          </table>
        </div>

      </div>
    </div>
  </div>
</body>
</html>